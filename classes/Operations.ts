export default class Operations {
  static sum(number1: number, number2: number): number {
    return number1 + number2;
  }

  static subtract(number1: number, number2: number): number {
    return number1 - number2;
  }
}
